# Sample Repo

Tiny fixture application used by the CodeAtlas test-suite.
Contains deliberately vulnerable code (SQL injection, fake AWS example keys)
so the security scanners have something to find. Never use it as a template.
